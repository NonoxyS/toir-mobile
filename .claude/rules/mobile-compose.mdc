---
description: Compose — recomposition optimization, composable splitting, screen structure
globs: **/*.{kt,kts}
alwaysApply: false
---

# Compose

## Recomposition Optimization

```kotlin
@Immutable
data class Ui{Feature}State(
    val value: String = "",
    val isLoading: Boolean = false,
)
```

### rememberUpdatedState for callbacks in effects

Required for callbacks inside `LaunchedEffect` / `DisposableEffect` to always have the latest instance without restarting the effect:

```kotlin
@Composable
private fun SomeEffect(onAction: () -> Unit) {
    val currentOnAction by rememberUpdatedState(onAction)

    LaunchedEffect(Unit) {
        // use currentOnAction() — always fresh, no effect restart
    }
}
```

### remember for modifier depending on parameters

```kotlin
val modifier = remember(paddingValues) { Modifier.padding(paddingValues) }
```

### derivedStateOf for derived state

Use when state is computed from another state and recomposition on every source change is not needed:

```kotlin
val showButton by remember {
    derivedStateOf { lazyListState.firstVisibleItemIndex > 0 }
}
```

### key for LazyColumn items

Always provide `key` for `LazyColumn` items:

```kotlin
LazyColumn {
    items(items = items, key = { it.id }) { item ->
        ItemCard(item = item)
    }
}
```

## Composable Components (splitting rules)

### Pass minimum required data

Pass only what child composables need — not the entire state object:

```kotlin
// ✅ Correct
{Feature}Content(
    value = state.value,
    isLoading = state.isLoading,
    onNextClick = onNextClick,
)

// ❌ Wrong
{Feature}Content(state = state)
```

### Spacer extensions

Always use Spacer extensions from `ru.mirea.toir.common.ui.compose.utils` instead of raw `Spacer(modifier = Modifier.height/width(Ndp))`:

```kotlin
// ✅ Column — vertical gap
Column {
    Spacer8()
    Spacer12()
}
// ✅ Row — horizontal gap
Row { Spacer8() }

// ❌ Wrong
Spacer(modifier = Modifier.height(8.dp))
Spacer(modifier = Modifier.width(8.dp))
```

Available: `Spacer4`, `Spacer8`, `Spacer10`, `Spacer12`, `Spacer16`, `Spacer24`, `Spacer32`, `Spacer48` (both `ColumnScope` and `RowScope`).

### LaunchedEffect / SideEffect placement

Place at the **beginning** of the composable function, before UI content. Extract complex effects into separate composables.

## Preview for UI Components

Every composable component in UI modules must have a `@Preview` function in the same file.

Rules:
- Name: `Preview` + component name (e.g. `PreviewItemCard`)
- Always `@Preview(showBackground = true)`
- Wrapped in `KmmTemplateTheme { }`
- For components with multiple states — show variants in a `Column`

```kotlin
@Preview(showBackground = true)
@Composable
private fun PreviewItemCard() {
    KmmTemplateTheme {
        Column(
            modifier = Modifier.padding(16.dp),
            verticalArrangement = Arrangement.spacedBy(12.dp),
        ) {
            ItemCard(/* example data */)
        }
    }
}
```

## Components Package

Sub-composables (Content, Loading, Empty, Error, Items, Card) go in a `components/` sub-package inside the `ui/` module with `internal` visibility. Only `{Feature}Screen` and `{Feature}TopBar` stay at the `ui/` package root.

```
ui/
  {Feature}Screen.kt       // internal: Screen + TopBar only
  components/
    {Feature}Content.kt    // internal: state dispatcher + sub-states
    {Feature}Card.kt       // internal: list item + private sub-composables
```

## No Logic in UI Composables

UI composables must not compute derived values. Percentages, formatted strings, and combined flags must be precomputed in the mapper and stored as fields in the UI model.

```kotlin
// ✅ Correct — computed in mapper, stored in UiModel
LinearProgressIndicator(progress = { item.progress })

// ❌ Wrong — logic in composable
val progress = if (item.totalPoints > 0) item.completedPoints / item.totalPoints else 0f
```
